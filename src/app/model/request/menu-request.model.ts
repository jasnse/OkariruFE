export interface addMenuRequest {
  namaMenu: string;
  deskripsiMenu: string;
  path: string;
  icon: string;
}

export interface updateMenu {
  namaMenu: string;
  deskripsiMenu: string;
  path: string;
  icon: string;
}